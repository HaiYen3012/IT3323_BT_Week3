PROGRAM TestAssign2
VAR a : INTEGER;
VAR b : INTEGER;
VAR sum : INTEGER;

FUNCTION Add(x : INTEGER; y : INTEGER) : INTEGER;
VAR temp : INTEGER;
BEGIN
  temp := x + y;
  Add := temp
END;

BEGIN
  WRITEI(a);
  WRITEI(b);
  sum := Add(a, b);
  WRITEI(sum)
END.
